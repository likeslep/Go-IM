## 用户表：
```sql
CREATE TABLE `users` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` varchar(50) NOT NULL COMMENT '账号',
  `password` varchar(100) NOT NULL COMMENT '密码（加密）',
  `nickname` varchar(50) DEFAULT NULL COMMENT '昵称',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `email` varchar(50) DEFAULT NULL COMMENT '邮箱',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态 1在线 2离线 3隐身',
  `created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

## 好友申请表：
```sql
CREATE TABLE `user_apply` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `from_uid` bigint NOT NULL COMMENT '申请人ID',
  `to_uid` bigint NOT NULL COMMENT '接收人ID',
  `remark` varchar(100) DEFAULT NULL COMMENT '申请备注',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '0待处理 1同意 2拒绝',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `from_uid` (`from_uid`),
  KEY `to_uid` (`to_uid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='好友申请表';
```

## 好友关系表：
```sql
CREATE TABLE `friend` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `uid` bigint NOT NULL COMMENT '用户ID',
  `friend_uid` bigint NOT NULL COMMENT '好友ID',
  `group_name` varchar(50) DEFAULT '我的好友' COMMENT '分组名',
  `remark` varchar(50) DEFAULT NULL COMMENT '好友备注',
  `is_black` tinyint DEFAULT '0' COMMENT '0否 1拉黑',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uid_friend` (`uid`,`friend_uid`),
  KEY `friend_uid` (`friend_uid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='好友关系表';
```

## 群聊表：
```sql
CREATE TABLE `group` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '群ID',
  `name` varchar(100) NOT NULL COMMENT '群名称',
  `avatar` varchar(255) DEFAULT NULL COMMENT '群头像',
  `owner_uid` bigint NOT NULL COMMENT '群主ID',
  `notice` text COMMENT '群公告',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='群聊表';
```

## 群聊成员表：
```sql
CREATE TABLE `group_member` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `group_id` bigint NOT NULL,
  `uid` bigint NOT NULL,
  `role` tinyint NOT NULL DEFAULT '3' COMMENT '1群主 2管理员 3普通成员',
  `nickname` varchar(50) DEFAULT NULL COMMENT '群昵称',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `group_uid` (`group_id`,`uid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='群成员表';
```

## 消息表：
```sql
CREATE TABLE `message` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '消息ID',
  `type` tinyint NOT NULL COMMENT '1单聊 2群聊',
  `from_uid` bigint NOT NULL COMMENT '发送者',
  `to_uid` bigint NOT NULL COMMENT '接收者/群ID',
  `content` text COMMENT '消息内容',
  `msg_type` tinyint NOT NULL DEFAULT '1' COMMENT '1文本 2图片 3文件 4语音',
  `is_revoke` tinyint DEFAULT '0' COMMENT '0正常 1撤回',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `from_uid` (`from_uid`),
  KEY `to_uid` (`to_uid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='消息表';
```

## 会话表：
```go
CREATE TABLE `conversation` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `uid` bigint NOT NULL COMMENT '所属用户',
  `type` tinyint NOT NULL COMMENT '1单聊 2群聊',
  `to_id` bigint NOT NULL COMMENT '对方ID/群ID',
  `unread_num` int DEFAULT '0' COMMENT '未读消息数',
  `last_msg` varchar(255) DEFAULT NULL COMMENT '最后一条消息',
  `last_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_top` tinyint DEFAULT '0' COMMENT '是否置顶',
  `is_mute` tinyint DEFAULT '0' COMMENT '是否免打扰',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uid_conversation` (`uid`,`type`,`to_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会话表';
```

## 文件上传表：
```sql
CREATE TABLE `file_upload` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `file_name` varchar(255) NOT NULL,
  `file_path` varchar(255) NOT NULL,
  `file_size` bigint DEFAULT '0',
  `upload_uid` bigint NOT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文件上传表';
```


eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoxLCJleHAiOjE3Nzc3MTc0OTMsImlhdCI6MTc3NzExMjY5M30.WsUuud26HNDRCKs6CxdGxzFsVYeoVV7YP6whLN9AqP0



eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoyLCJleHAiOjE3Nzc3MjY5OTYsImlhdCI6MTc3NzEyMjE5Nn0.X5AEFCdI-Fa2HEB3x4oV6dn5K60X4nQYxCXfwQ4ypC8